SELECT 
    P.YEAR AS YEAR, 
    UPPER(DATENAME(MONTH, DATEFROMPARTS(P.YEAR, P.MONTH, 1)))  AS MONTH,
    (P.YEAR - 1) AS LAST_YEAR,
    FORMAT(GETDATE(), 'dd-MMMM-yyyy') EXPORT_DATE
FROM
    AT_SALARY_PERIOD P
WHERE
    P.ID = 16;

kết quả trả về
2025 |	AUGUST |	2024	| 30-September-2025
